
{{ set(this, 'title', t(module('NEWS'), 'Edit news record')) }}

<div class="row margin-bottom-15">
    <div class="col-xs-12">
        <div class="btn-group pull-right">
            <a href="{{ url({0: route('SITE_NEWS_SHOW'), 'id': newsRecord.id, 'slug': newsRecord.slug}) }}" class="btn btn-default">
                {{ t(module('NEWS'), 'News record page') }}
            </a>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-xs-12">
        <div class="panel panel-default">
            <div class="panel-body">
                {{ render('news/_form')|raw }}
            </div>
        </div>
    </div>
</div>
